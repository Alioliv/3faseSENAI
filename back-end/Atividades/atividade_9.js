/*9 — Busca Binária Recursiva
Atividades Estrutura de dados 5
Comparar versão iterativa vs recursiva.
Complexidade Esperada
Tempo: O(log n)
Espaço (pilha recursiva): O(log n)*/

function buscaBinariaRecursiva(arr, numeroBuscado, inicio = 0, fim = arr.length - 1) {
    if (inicio > fim) return -1 
    const meio = inicio// Número não encontrado
    
}